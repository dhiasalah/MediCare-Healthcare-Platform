from django.apps import AppConfig


class HealthPredictionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'health_predictions'
    verbose_name = 'Health Predictions'
